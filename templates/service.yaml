apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubemancer-chart.fullname" . }}
  labels:
    {{- include "kubemancer-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubemancer-chart.selectorLabels" . | nindent 4 }}
