apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubemancer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kubemancer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubemancer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
